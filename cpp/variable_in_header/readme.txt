can declare global variable in header

can define static variable in header protected by #ifndef, in this case, every source file including this header has different copy of this static variable

can not declare extern variable in header.
